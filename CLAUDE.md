Architecture and implementation of FTS is described in PPTX you can find in docs/fts-presa-*.pptx.
You better use bin/pptx_to_markdown.py script to extract bullet points with markup.
There is a wrapper around idf.py at bin/idf which should be used to build the project.